---
import BookingModal from '../components/BookingModal.astro';

export interface Props {
  title?: string;
  description?: string;
  canonical?: string;
}

const { 
  title = 'Nails by Sara Sofie - Negletekniker i Son', 
  description = 'Negletekniker i Son med flere behandlinger til gode priser. Gelenegler, gellakk og negldesign hos Nails by Sara Sofie. Book time online - Plankesvingen 2, 1555 Son.',
  canonical
} = Astro.props;

const canonicalURL = canonical || new URL(Astro.url.pathname, Astro.site);

// Strukturerte data for LocalBusiness/BeautySalon
const structuredData = {
  "@context": "https://schema.org",
  "@type": ["LocalBusiness", "BeautySalon"],
  "name": "Nails by Sara Sofie",
  "description": "Neglesalong i Son med gode priser. Gelenegler, gellakk og negldesign. Bestill time enkelt online.",
  "url": "https://nailsbysarasofie.no",
  "logo": {
    "@type": "ImageObject",
    "url": "https://nailsbysarasofie.no/logo.jpg",
    "width": 400,
    "height": 400
  },
  "image": [
    {
      "@type": "ImageObject",
      "url": "https://nailsbysarasofie.no/logo.jpg",
      "width": 1200,
      "height": 630,
      "description": "Nails by Sara Sofie - Neglesalong i Son"
    },
    {
      "@type": "ImageObject", 
      "url": "https://nailsbysarasofie.no/galleri/gelenegler/gelenegler_1.jpg",
      "width": 800,
      "height": 600,
      "description": "Gelenegler behandling hos Nails by Sara Sofie"
    },
    {
      "@type": "ImageObject",
      "url": "https://nailsbysarasofie.no/galleri/gellakk/gellakk_1.jpg", 
      "width": 800,
      "height": 600,
      "description": "Gellakk behandling hos Nails by Sara Sofie"
    }
  ],
  "telephone": "",
  "email": "post@nailsbysarasofie.no",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "Plankesvingen 2",
    "addressLocality": "Son",
    "postalCode": "1555",
    "addressRegion": "Viken",
    "addressCountry": "NO"
  },
  "geo": {
    "@type": "GeoCoordinates",
    "latitude": "59.51543797470495",
    "longitude": "10.68361047725128"
  },
  "areaServed": [
    {
      "@type": "City",
      "name": "Son"
    },
    {
      "@type": "City", 
      "name": "Moss"
    },
    {
      "@type": "City",
      "name": "Vestby"
    }
  ],
  "serviceType": ["Gelenegler", "Gellakk", "Negldesign", "Neglpleie"],
  "priceRange": "80-450 NOK",
  "paymentAccepted": "Cash, Card",
  "currenciesAccepted": "NOK",
  "founder": {
    "@type": "Person",
    "name": "Sara Sofie"
  },
  "sameAs": [
    "https://www.instagram.com/nails_sarasofie"
  ],
  "url": "https://nailsbysarasofie.no",
  "mainEntityOfPage": "https://nailsbysarasofie.no",
  "potentialAction": [
    {
      "@type": "ReserveAction",
      "name": "Book time",
      "target": {
        "@type": "EntryPoint",
        "urlTemplate": "https://www.fresha.com/nb/book-now/nails-by-sara-sofie-aoar71eq/services?lid=2696667&eid=4757759&share=true&pId=2609801",
        "actionPlatform": [
          "http://schema.org/DesktopWebPlatform",
          "http://schema.org/MobileWebPlatform"
        ]
      },
      "result": {
        "@type": "Reservation",
        "name": "Neglbehandling reservasjon"
      }
    }
  ],
  "hasOfferCatalog": {
    "@type": "OfferCatalog",
    "name": "Negltjenester",
    "itemListElement": [
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service",
          "name": "Gelenegler, nytt sett",
          "description": "Helt nye gelenegler med ønsket lengde og design",
          "image": "https://nailsbysarasofie.no/galleri/gelenegler/gelenegler_1.jpg"
        },
        "price": "400",
        "priceCurrency": "NOK",
        "availability": "http://schema.org/InStock",
        "url": "https://www.fresha.com/nb/book-now/nails-by-sara-sofie-aoar71eq/services?lid=2696667&eid=4757759&share=true&pId=2609801"
      },
      {
        "@type": "Offer", 
        "itemOffered": {
          "@type": "Service",
          "name": "Gelenegler, påfyll",
          "description": "Vedlikehold og påfyll av eksisterende gelenegler",
          "image": "https://nailsbysarasofie.no/galleri/gelenegler/gelenegler_2.jpg"
        },
        "price": "300",
        "priceCurrency": "NOK",
        "availability": "http://schema.org/InStock",
        "url": "https://www.fresha.com/nb/book-now/nails-by-sara-sofie-aoar71eq/services?lid=2696667&eid=4757759&share=true&pId=2609801"
      },
      {
        "@type": "Offer",
        "itemOffered": {
          "@type": "Service", 
          "name": "Gellakk, nytt sett",
          "description": "Langtidsholdbar lakk på naturlige negler",
          "image": "https://nailsbysarasofie.no/galleri/gellakk/gellakk_1.jpg"
        },
        "price": "200",
        "priceCurrency": "NOK",
        "availability": "http://schema.org/InStock",
        "url": "https://www.fresha.com/nb/book-now/nails-by-sara-sofie-aoar71eq/services?lid=2696667&eid=4757759&share=true&pId=2609801"
      }
    ]
  }
};
---

<!DOCTYPE html>
<html lang="no">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <title>{title}</title>
    <meta name="description" content={description} />
    
    <!-- Content Security Policy -->
    <meta http-equiv="Content-Security-Policy" content="
      default-src 'self';
      script-src 'self' 'unsafe-inline' https://www.googletagmanager.com https://www.google-analytics.com;
      style-src 'self' 'unsafe-inline' https://fonts.googleapis.com;
      font-src 'self' https://fonts.gstatic.com;
      img-src 'self' data: https:;
      connect-src 'self' https://www.google-analytics.com;
      frame-src 'self' https://www.fresha.com https://www.google.com;
      object-src 'none';
      base-uri 'self';
      form-action 'self';
    " />
    
    <!-- Mobile optimization -->
    <meta name="theme-color" content="#F3C7D7" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="default" />
    <meta name="apple-mobile-web-app-title" content="Nails by Sara Sofie" />
    <meta name="format-detection" content="telephone=no" />
    
    <!-- Canonical URL -->
    <link rel="canonical" href={canonicalURL} />
    
    <!-- Local SEO Meta Tags -->
    <meta name="geo.region" content="NO-02" />
    <meta name="geo.placename" content="Son, Viken" />
    <meta name="geo.position" content="59.51543797470495;10.68361047725128" />
    <meta name="ICBM" content="59.51543797470495, 10.68361047725128" />
    
    <!-- Keywords for local SEO -->
    <meta name="keywords" content="neglesalong Son, gelenegler Son, gellakk Son, neglbehandling Son, nails Son, neglstudio Viken, neglesalong Moss, negldesign Son" />
    
    <!-- Additional metadata for rich snippets -->
    <meta name="author" content="Nails by Sara Sofie" />
    <meta name="robots" content="index, follow, max-image-preview:large, max-snippet:-1, max-video-preview:-1" />
    <meta name="googlebot" content="index, follow, max-video-preview:-1, max-image-preview:large, max-snippet:-1" />
    
    <!-- Business information -->
    <meta name="business:contact_data:street_address" content="Plankesvingen 2" />
    <meta name="business:contact_data:locality" content="Son" />
    <meta name="business:contact_data:region" content="Viken" />
    <meta name="business:contact_data:postal_code" content="1555" />
    <meta name="business:contact_data:country_name" content="Norway" />
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website" />
    <meta property="og:url" content={canonicalURL} />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content={new URL('logo.jpg', Astro.site)} />
    <meta property="og:image:alt" content="Nails by Sara Sofie - Logo" />
    <meta property="og:image:type" content="image/jpeg" />
    <meta property="og:image:width" content="1200" />
    <meta property="og:image:height" content="630" />
    <!-- Additional images for rich previews -->
    <meta property="og:image" content={new URL('galleri/gelenegler/gelenegler_1.jpg', Astro.site)} />
    <meta property="og:image:alt" content="Gelenegler behandling - Nails by Sara Sofie" />
    <meta property="og:image" content={new URL('galleri/gellakk/gellakk_1.jpg', Astro.site)} />
    <meta property="og:image:alt" content="Gellakk behandling - Nails by Sara Sofie" />
    <meta property="og:locale" content="nb_NO" />
    <meta property="og:site_name" content="Nails by Sara Sofie" />
    
    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image" />
    <meta property="twitter:url" content={canonicalURL} />
    <meta property="twitter:title" content={title} />
    <meta property="twitter:description" content={description} />
    <meta property="twitter:image" content={new URL('logo.jpg', Astro.site)} />
    <meta property="twitter:image:alt" content="Nails by Sara Sofie - Logo" />
    
    <!-- Favicon -->
    <link rel="icon" type="image/jpeg" href={`${import.meta.env.BASE_URL}logo.jpg`} sizes="any" />
    <link rel="icon" type="image/x-icon" href={`${import.meta.env.BASE_URL}favicon.ico`} />
    <link rel="apple-touch-icon" href={`${import.meta.env.BASE_URL}logo.jpg`} />
    <link rel="mask-icon" href={`${import.meta.env.BASE_URL}logo.jpg`} color="#F3C7D7" />
    
    <!-- Google Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600&family=Playfair+Display:wght@400;500;600&display=swap" rel="stylesheet" />
    
    <!-- Google Analytics 4 - For verifikasjon og GDPR-compliant tracking -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-TQKDPTFG2K"></script>
    <script>
      try {
        // Global site tag setup
        window.dataLayer = window.dataLayer || [];
        function gtag(){dataLayer.push(arguments);}
        window.gtag = gtag;
        
        // GDPR-compliant: Start med consent DENIED
        gtag('consent', 'default', {
          'analytics_storage': 'denied',
          'ad_storage': 'denied',
          'ad_user_data': 'denied',
          'ad_personalization': 'denied'
        });
        
        gtag('js', new Date());
        gtag('config', 'G-TQKDPTFG2K', {
          'anonymize_ip': true,
          'allow_google_signals': false,
          'allow_ad_personalization_signals': false
        });
        
        // Google Analytics loaded (tracking disabled until consent)
      } catch (error) {
        // Google Analytics not available
      }
      
      // Enkel og pålitelig cookie-samtykke løsning
      
      // Aktiver Google Analytics tracking
      window.loadGoogleAnalytics = function() {
        try {
          if (typeof window.gtag === 'function') {
            gtag('consent', 'update', {
              'analytics_storage': 'granted'
            });
            // Google Analytics tracking enabled
          }
        } catch (error) {
          // Google Analytics not available
        }
      };

      window.disableGoogleAnalytics = function() {
        try {
          if (typeof window.gtag === 'function') {
            gtag('consent', 'update', {
              'analytics_storage': 'denied',
              'ad_storage': 'denied',
              'ad_user_data': 'denied',
              'ad_personalization': 'denied'
            });
          }
          
          // Slett eksisterende Google Analytics cookies
          const cookies = ['_ga', '_gid', '_gat_gtag_G_TQKDPTFG2K'];
          cookies.forEach(name => {
            document.cookie = `${name}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/; domain=.${window.location.hostname};`;
            document.cookie = `${name}=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;`;
          });
          
          // Google Analytics tracking disabled
        } catch (error) {
          // Google Analytics not available
        }
      };

      // Cookie banner funksjonalitet
      function showCookieBanner() {
        try {
          const banner = document.getElementById('cookie-banner');
          if (banner) {
            banner.style.display = 'block';
            banner.style.animation = 'slideUp 0.5s ease-out';
          }
        } catch (error) {
          // Cookie banner not available
        }
      }

      function hideCookieBanner() {
        try {
          const banner = document.getElementById('cookie-banner');
          if (banner) {
            banner.style.animation = 'slideDown 0.5s ease-out';
            setTimeout(() => {
              banner.style.display = 'none';
            }, 500);
          }
        } catch (error) {
          // Cookie banner not available
        }
      }

      window.acceptCookies = function() {
        try {
          localStorage.setItem('cookie-consent', 'accepted');
          localStorage.setItem('cookie-consent-date', new Date().toISOString());
          hideCookieBanner();
          window.loadGoogleAnalytics();
          // Cookies accepted
        } catch (error) {
          // Cookie functionality not available
        }
      };

      window.declineCookies = function() {
        try {
          localStorage.setItem('cookie-consent', 'declined');
          localStorage.setItem('cookie-consent-date', new Date().toISOString());
          hideCookieBanner();
          window.disableGoogleAnalytics();
          // Cookies declined
        } catch (error) {
          // Cookie functionality not available
        }
      };

      window.showCookieSettings = function() {
        try {
          // Fjern eksisterende samtykke og vis banner igjen
          localStorage.removeItem('cookie-consent');
          showCookieBanner();
        } catch (error) {
          // Cookie settings not available
        }
      };

      // Cookie details modal functions
      window.showCookieDetails = function() {
        try {
          const modal = document.getElementById('cookie-details-modal');
          if (modal) {
            modal.style.display = 'flex';
            document.body.style.overflow = 'hidden';
          }
        } catch (error) {
          // Cookie modal not available
        }
      };

      window.hideCookieDetails = function() {
        try {
          const modal = document.getElementById('cookie-details-modal');
          if (modal) {
            modal.style.display = 'none';
            document.body.style.overflow = '';
          }
        } catch (error) {
          // Cookie modal not available
        }
      };

      window.acceptCookiesAndClose = function() {
        try {
          window.acceptCookies();
          window.hideCookieDetails();
        } catch (error) {
          // Cookie functionality not available
        }
      };

      window.declineCookiesAndClose = function() {
        try {
          window.declineCookies();
          window.hideCookieDetails();
        } catch (error) {
          // Cookie functionality not available
        }
      };

      // Initialiser når DOM er klar
      document.addEventListener('DOMContentLoaded', function() {
        try {
          const consent = localStorage.getItem('cookie-consent');
          
          if (consent === 'accepted') {
            window.loadGoogleAnalytics();
          } else if (consent === 'declined') {
            window.disableGoogleAnalytics();
          } else {
            // Vis banner etter 2 sekunder
            setTimeout(showCookieBanner, 2000);
          }

          // Cookie button event listeners
          const cookieDetailsBtn = document.querySelector('.cookie-details-btn');
          const cookieAcceptBtn = document.querySelector('.cookie-accept');
          const cookieDeclineBtn = document.querySelector('.cookie-decline');
          const cookieModalBackdrop = document.querySelector('.cookie-modal-backdrop');
          const cookieModalClose = document.querySelector('.cookie-modal-close');
          const cookieAcceptPrimary = document.querySelector('.cookie-accept-primary');
          const cookieDeclineSecondary = document.querySelector('.cookie-decline-secondary');

          if (cookieDetailsBtn) {
            cookieDetailsBtn.addEventListener('click', function() {
              try {
                window.showCookieDetails();
              } catch (error) {
                // Cookie details not available
              }
            });
          }

          if (cookieAcceptBtn) {
            cookieAcceptBtn.addEventListener('click', function() {
              try {
                window.acceptCookies();
              } catch (error) {
                // Cookie accept not available
              }
            });
          }

          if (cookieDeclineBtn) {
            cookieDeclineBtn.addEventListener('click', function() {
              try {
                window.declineCookies();
              } catch (error) {
                // Cookie decline not available
              }
            });
          }

          if (cookieModalBackdrop) {
            cookieModalBackdrop.addEventListener('click', function() {
              try {
                window.hideCookieDetails();
              } catch (error) {
                // Cookie modal not available
              }
            });
          }

          if (cookieModalClose) {
            cookieModalClose.addEventListener('click', function() {
              try {
                window.hideCookieDetails();
              } catch (error) {
                // Cookie modal not available
              }
            });
          }

          if (cookieAcceptPrimary) {
            cookieAcceptPrimary.addEventListener('click', function() {
              try {
                window.acceptCookiesAndClose();
              } catch (error) {
                // Cookie accept not available
              }
            });
          }

          if (cookieDeclineSecondary) {
            cookieDeclineSecondary.addEventListener('click', function() {
              try {
                window.declineCookiesAndClose();
              } catch (error) {
                // Cookie decline not available
              }
            });
          }

          // Footer cookie settings button
          const footerCookieSettingsBtn = document.querySelector('button[aria-label="Åpne cookie-innstillinger"]');
          if (footerCookieSettingsBtn) {
            footerCookieSettingsBtn.addEventListener('click', function() {
              try {
                if (typeof window.showCookieSettings === 'function') {
                  window.showCookieSettings();
                }
              } catch (error) {
                // Cookie settings not available
              }
            });
          }
        } catch (error) {
          // Cookie functionality not available
        }
      });
    </script>
    
    <!-- Structured Data -->
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  </head>
  <body>
    <slot />
    
    <!-- Booking Modal -->
    <BookingModal />
    
    <!-- Minimal Cookie Banner -->
    <div id="cookie-banner" class="cookie-banner" style="display: none;">
      <div class="cookie-banner-content">
        <div class="cookie-text">
          <span class="cookie-icon">🍪</span>
          <span class="cookie-message">Vi bruker cookies for å forbedre din opplevelse.</span>
          <button class="cookie-details-btn" aria-label="Les mer om cookies">
            Innstillinger
          </button>
        </div>
        <div class="cookie-actions">
                  <button class="cookie-accept" aria-label="Godta cookies">Godta</button>
        <button class="cookie-decline" aria-label="Avvis cookies">Avvis</button>
        </div>
      </div>
    </div>

    <!-- Cookie Details Modal -->
    <div id="cookie-details-modal" class="cookie-modal" style="display: none;">
      <div class="cookie-modal-backdrop"></div>
      <div class="cookie-modal-content">
        <div class="cookie-modal-header">
          <h2>🍪 Informasjonskapsler og personvern</h2>
          <button class="cookie-modal-close" aria-label="Lukk">&times;</button>
        </div>
        <div class="cookie-modal-body">
          <p>
            Vi bruker Google Analytics for å forstå hvordan besøkende bruker nettsiden og forbedre brukeropplevelsen. 
            Informasjonen behandles anonymt og i tråd med GDPR.
          </p>
          <p>
            Du kan når som helst endre dine preferanser ved å klikke på "Cookie-innstillinger" nederst på siden.
          </p>
        </div>
        <div class="cookie-modal-actions">
                  <button class="cookie-accept-primary" aria-label="Godta alle cookies">Godta alle</button>
        <button class="cookie-decline-secondary" aria-label="Avvis alle cookies">Avvis alle</button>
        </div>
      </div>
    </div>

         <style>
       /* Cookie Banner Styles */
       .cookie-banner {
         position: fixed;
         bottom: 0;
         left: 0;
         right: 0;
         background: #1a1a1a;
         color: #ffffff;
         z-index: 9998;
         border-top: 2px solid #F3C7D7;
         animation: slideUp 0.3s ease-out;
       }

       .cookie-banner-content {
         max-width: 1200px;
         margin: 0 auto;
         padding: 0.75rem 1rem;
         display: flex;
         align-items: center;
         justify-content: space-between;
         gap: 1rem;
         flex-wrap: wrap;
       }

       .cookie-text {
         display: flex;
         align-items: center;
         gap: 0.75rem;
         flex: 1;
         min-width: 250px;
       }

       .cookie-icon {
         font-size: 1.2rem;
       }

       .cookie-message {
         font-size: 0.9rem;
         font-family: 'Inter', sans-serif;
         color: #ffffff;
       }

       .cookie-details-btn {
         background: none;
         border: none;
         color: #F3C7D7;
         text-decoration: underline;
         cursor: pointer;
         font-size: 0.8rem;
         font-family: 'Inter', sans-serif;
         padding: 0;
       }

       .cookie-details-btn:hover {
         color: #ffffff;
       }

       .cookie-actions {
         display: flex;
         gap: 0.5rem;
         align-items: center;
       }

       .cookie-accept, .cookie-decline {
         padding: 0.4rem 0.8rem;
         border-radius: 0.5rem;
         font-size: 0.85rem;
         font-family: 'Inter', sans-serif;
         cursor: pointer;
         border: none;
         transition: all 0.2s;
       }

       .cookie-accept {
         background: #F3C7D7;
         color: #1a1a1a;
         font-weight: 500;
       }

       .cookie-accept:hover {
         background: #e8b6c9;
       }

       .cookie-decline {
         background: transparent;
         color: #cccccc;
         border: 1px solid #cccccc;
       }

       .cookie-decline:hover {
         color: #ffffff;
         border-color: #ffffff;
       }

       /* Cookie Modal Styles */
       .cookie-modal {
         position: fixed;
         top: 0;
         left: 0;
         right: 0;
         bottom: 0;
         z-index: 10000;
         display: flex;
         align-items: center;
         justify-content: center;
         padding: 1rem;
       }

       .cookie-modal-backdrop {
         position: absolute;
         top: 0;
         left: 0;
         right: 0;
         bottom: 0;
         background: rgba(0, 0, 0, 0.7);
         backdrop-filter: blur(4px);
       }

       .cookie-modal-content {
         background: #ffffff;
         border-radius: 1rem;
         max-width: 500px;
         width: 100%;
         position: relative;
         box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
       }

       .cookie-modal-header {
         display: flex;
         justify-content: space-between;
         align-items: center;
         padding: 1.5rem 1.5rem 1rem;
         border-bottom: 1px solid #f0f0f0;
       }

       .cookie-modal-header h2 {
         margin: 0;
         font-size: 1.2rem;
         font-family: 'Playfair Display', serif;
         color: #1a1a1a;
       }

       .cookie-modal-close {
         background: none;
         border: none;
         font-size: 1.5rem;
         cursor: pointer;
         color: #666;
         padding: 0;
         width: 2rem;
         height: 2rem;
         display: flex;
         align-items: center;
         justify-content: center;
       }

       .cookie-modal-close:hover {
         color: #1a1a1a;
       }

       .cookie-modal-body {
         padding: 1rem 1.5rem;
       }

       .cookie-modal-body p {
         margin: 0 0 1rem 0;
         line-height: 1.5;
         color: #4a4a4a;
         font-family: 'Inter', sans-serif;
         font-size: 0.9rem;
       }

       .cookie-modal-body p:last-child {
         margin-bottom: 0;
       }

       .cookie-modal-actions {
         padding: 1rem 1.5rem 1.5rem;
         display: flex;
         gap: 0.75rem;
         justify-content: flex-end;
       }

       .cookie-accept-primary, .cookie-decline-secondary {
         padding: 0.75rem 1.5rem;
         border-radius: 0.75rem;
         font-family: 'Inter', sans-serif;
         font-size: 0.9rem;
         cursor: pointer;
         border: none;
         transition: all 0.2s;
       }

       .cookie-accept-primary {
         background: #F3C7D7;
         color: #1a1a1a;
         font-weight: 500;
       }

       .cookie-accept-primary:hover {
         background: #e8b6c9;
       }

       .cookie-decline-secondary {
         background: transparent;
         color: #666;
         border: 1px solid #ddd;
       }

       .cookie-decline-secondary:hover {
         color: #1a1a1a;
         border-color: #aaa;
       }

       @keyframes slideUp {
         from { transform: translateY(100%); opacity: 0; }
         to { transform: translateY(0); opacity: 1; }
       }

       @keyframes slideDown {
         from { transform: translateY(0); opacity: 1; }
         to { transform: translateY(100%); opacity: 0; }
       }

       @media (max-width: 768px) {
         .cookie-banner-content {
           flex-direction: column;
           align-items: flex-start;
           gap: 0.75rem;
         }

         .cookie-text {
           flex-direction: column;
           align-items: flex-start;
           gap: 0.5rem;
         }

         .cookie-actions {
           align-self: stretch;
           justify-content: space-between;
         }

         .cookie-accept, .cookie-decline {
           flex: 1;
         }
       }
     </style>
     
     <!-- Google Analytics Event Tracking -->
     <script>
       document.addEventListener('DOMContentLoaded', function() {
         try {
           // Analytics event tracking loaded
           
           // Track all booking links (Fresha)
           const bookingLinks = document.querySelectorAll('a[href*="fresha.com"]');
           
           bookingLinks.forEach(link => {
             link.addEventListener('click', function() {
               // Google Analytics event tracking (kun hvis aktivert via samtykke)
               try {
                 if (typeof window.gtag !== 'undefined' && window.gtag) {
                   window.gtag('event', 'booking_click', {
                     'event_category': 'booking',
                     'event_label': 'fresha_link',
                     'transport_type': 'beacon'
                   });
                   // Analytics: Booking link clicked (Fresha)
                 }
               } catch (error) {
                 // Analytics not available
               }
             });
           });
           
           // Tracking booking links
         } catch (error) {
           // Analytics tracking not available
         }
       });
     </script>
   </body>
 </html> 